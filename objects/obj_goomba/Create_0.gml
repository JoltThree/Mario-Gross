// Movement configuration
move_speed = 1;             // How fast the Goomba walks
h_speed = -move_speed;        // Start moving left
v_speed = 0;                  // Vertical speed
grav = 0.5;                   // Gravity strength
is_dead = false
// Ledge behavior toggle
turn_at_ledges = true;        // Set to true to stop them from falling